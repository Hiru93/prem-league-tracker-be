import { Injectable } from '@nestjs/common';

// The scoring formula and standings aggregation land in Phase 3 — see
// docs/league-scoring-technical.md. This module is scaffolded empty; no
// controller of its own (consumed by StagesModule/PlayersModule per
// docs/backend-architecture-technical.md).
@Injectable()
export class ScoringService {}
